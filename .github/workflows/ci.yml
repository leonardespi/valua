name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1

jobs:
  test:
    name: Test (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os:
          - ubuntu-latest       # Linux x86_64
          - ubuntu-24.04-arm    # Linux aarch64
          - macos-latest        # macOS aarch64 (Apple Silicon)
          - windows-latest      # Windows x86_64

    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy

      - name: Cache cargo registry
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-${{ runner.arch }}-cargo-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: ${{ runner.os }}-${{ runner.arch }}-cargo-

      - name: Install LuaJIT (Linux)
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update -q
          sudo apt-get install -y luajit
        continue-on-error: true

      - name: Install LuaJIT (macOS)
        if: runner.os == 'macOS'
        run: brew install luajit
        continue-on-error: true

      - name: Install LuaJIT (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          choco install luajit --yes --no-progress
          # Ensure the choco-managed binary directory is on PATH for subsequent steps.
          Add-Content $env:GITHUB_PATH "C:\ProgramData\chocolatey\bin"
        continue-on-error: true

      - name: Check formatting
        run: cargo fmt --all -- --check

      - name: Clippy
        run: >
          cargo clippy --all-targets --all-features --
          -D warnings
          -W clippy::pedantic
          -A clippy::module_name_repetitions
          -A clippy::missing_errors_doc

      - name: Test
        run: cargo test --workspace
